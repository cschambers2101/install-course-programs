# Script to install useful programs

Use this script to install some useful programs to your PC. It will install and setup the i3 tiling window manager, tmux and a few developer tools

## To run the script

`bash install.sh`
